@model IEnumerable<Shop.Models.Manufacturer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
}

<h2>Producenci</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.ManufacturerID }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.ManufacturerID }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.ManufacturerID })
        </td>
    </tr>
}
    <tr>
        <td></td>
        <td></td>
        <td>
            @Html.ActionLink("Dodaj nowego", "Create")
        </td>
    </tr>
</table>
